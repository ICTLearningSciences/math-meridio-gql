version: 0.2

env:
  parameter-store:
    SERVERLESS_ACCESS_KEY: "/shared/serverless-cicd-token"

phases:
  install:
    runtime-versions:
      nodejs: 24

  build:
    commands:
      - echo Deploying stack
      - npm run deploy:dev
